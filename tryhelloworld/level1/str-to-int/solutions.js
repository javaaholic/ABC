/*
 * 스트링을 숫자로 바꾸기
 * http://tryhelloworld.co.kr/challenge_codes/110
 */

function strToInt1(str) {
  return +str;
}

function strToInt2(str) {
  return Number(str);
}

function strToInt3(str) {
  return parseInt(str);
}
